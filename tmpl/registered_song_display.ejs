<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>登録済み曲表示画面</title>
    <style>
        body{background: lightgray;}
    </style>
</head>


<body>

<br><br>

<div align="center">
    <a class="register" href="registration/">
        曲の新規登録を行う
    </a>
</div>

<br>

<div align="center">

    <table border="1">
        <caption><b>登録済みの曲</b></caption>
        <tr>
            <th>#</th>
            <th>曲名</th>
            <th>歌手名</th>
            <th>歌詞</th>
        </tr>
        <% boardList.forEach(function (value) { %>
        <tr>
            <td name="anchor<%= + value["id"] %>"><%= value["id"] %></td>
            <td><%= value["title"] %></td>
            <td><%= value["name"] %></td>
            <td><%= value["lyric"] %></td>
        </tr>
        <% }); %>
    </table>

</div>
</body>
</html>
